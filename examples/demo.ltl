// examples/demo.ltl — runnable demo for ltl_graphql_parse

import std::io
use ltl_graphql_parse as lib

fn main() {
    io::println("ltl_graphql_parse demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}